<form method="post" 
    class="btn btn-sm btn-outline-danger custom-btn p-0"
    action="{{ path('admin_period_to_subject_delete', {'id': period_to_subject.id}) }}" 
    onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ period_to_subject.id) }}">
    <button class="btn btn-sm btn-outline-danger w-100 h-100 border-0">Delete</button>
</form>
