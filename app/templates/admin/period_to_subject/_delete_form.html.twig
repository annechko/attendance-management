<form method="post" action="{{ path('admin_period_to_subject_delete', {'id': period_to_subject.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ period_to_subject.id) }}">
    <button class="btn">Delete</button>
</form>
