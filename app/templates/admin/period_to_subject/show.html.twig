{% extends 'menu-layout.html.twig' %}

{% block title %}PeriodToSubject{% endblock %}

{% block contents %}

    <table class="table">
        <tbody>
            <tr>
                <th class='bg-light col-2'>Id</th>
                <td>{{ period_to_subject.id }}</td>
            </tr>
            <tr>
                <th class='bg-light'>TotalNumberOfLessons</th>
                <td>{{ period_to_subject.totalNumberOfLessons }}</td>
            </tr>
            <tr>
            <th class='bg-light'>Subject</th>
                <td class='h4'>{{ period_to_subject.subject.name }}</td>
            </tr>
            <th class='bg-light'>Code</th>
                <td>{{ period_to_subject.subject.code }}</td>
            </tr>
            <th class='bg-light'>Period</th>
                <td>
                    <a  class="text-decoration-none"
                        href="{{ path('admin_period_show', {'id':period_to_subject.period.id}) }}">
                        {{ period_to_subject.period.name }}
                    </a>
                </td>
            </tr>
        </tbody>
    </table>

{% endblock %}

{% block actions %}
            <a class="btn custom-btn btn-sm btn-outline-dark"
                    href="{{ path('admin_period_to_subject_index') }}">Back
            </a>
            <a class="btn custom-btn btn-sm btn-outline-dark"
                href="{{ path('admin_period_to_subject_edit', {'id': period_to_subject.id}) }}">Edit
            </a>
    {{ include('admin/period_to_subject/_delete_form.html.twig') }}
{% endblock %}
