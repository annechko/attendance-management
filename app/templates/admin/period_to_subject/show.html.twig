{% extends 'menu-layout.html.twig' %}

{% block title %}PeriodToSubject{% endblock %}

{% block contents %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ period_to_subject.id }}</td>
            </tr>
            <tr>
                <th>TotalNumberOfLessons</th>
                <td>{{ period_to_subject.totalNumberOfLessons }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_period_to_subject_index') }}">back to list</a>

    <a href="{{ path('admin_period_to_subject_edit', {'id': period_to_subject.id}) }}">edit</a>

    {{ include('admin/period_to_subject/_delete_form.html.twig') }}
{% endblock %}
