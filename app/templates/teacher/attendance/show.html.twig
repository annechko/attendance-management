{% extends 'menu-layout.html.twig' %}

{% block title %}Attendance{% endblock %}

{% block contents %}
    <h1>Attendance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attendance.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ attendance.date ? attendance.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ attendance.status }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ attendance.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('teacher_attendance_index') }}">back to list</a>

    <a href="{{ path('teacher_attendance_edit', {'id': attendance.id}) }}">edit</a>

    {{ include('teacher/attendance/_delete_form.html.twig') }}
{% endblock %}
